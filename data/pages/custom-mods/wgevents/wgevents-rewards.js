window.WIKI_PAGES = window.WIKI_PAGES || {};

window.WIKI_PAGES["wgevents-rewards"] = {
  title: "WGEvents rewards and voting",
  summary: "Server-side rewards.yml automation and public next-match voting in WGEvents.",
  version: "WGEvents 1.0.0 source review",
  releaseState: "Live",
  lastUpdated: "2026-04-08",
  needsUpdate: false,
  updateMessage: "",
  body: `
        <h3>Rewards system</h3>
        <p>WGEvents includes a dedicated <code>RewardsService</code> backed by <code>config/wgevents/rewards.yml</code>. This is not hardcoded into a gamemode class; it is a separate automation layer that can react to kills, points, streaks, and match-start events.</p>

        <h3>What rewards.yml supports</h3>
        <p>The source shows a lightweight custom YAML-style parser that supports the structure generated by the default template. Supported action types are:</p>
        <ul>
          <li><strong>command</strong> — dispatch a server command</li>
          <li><strong>broadcast</strong> — send a formatted server message</li>
        </ul>

        <h3>Reward categories visible in the template/parser</h3>
        <ul>
          <li><strong>Kills</strong> — per-kill actions and numeric killstreak thresholds</li>
          <li><strong>Scores</strong> — per-point actions and numeric pointstreak thresholds</li>
          <li><strong>Broken</strong> — actions for breaking a victim’s qualifying streak</li>
          <li><strong>Start</strong> — match-start actions</li>
        </ul>

        <h3>Placeholders</h3>
        <p>The default rewards template documents placeholders including:</p>
        <ul>
          <li><code>{player}</code></li>
          <li><code>{victim}</code></li>
          <li><code>{points}</code></li>
          <li><code>{streak}</code></li>
          <li><code>{opstreak}</code></li>
          <li><code>{oppoints}</code></li>
        </ul>

        <h3>Runtime control through config</h3>
        <p>WGEvents also has boolean config switches for enabling or disabling different reward paths, including kill, killstreak, point, pointstreak, broken-streak, and match-start reward handling.</p>

        <h3>Voting system</h3>
        <p><code>VotingService</code> provides public mode voting while no match is running. The current source shows:</p>
        <ul>
          <li>Voting only while the server is in <code>IDLE</code></li>
          <li>Automatic opening once the minimum online player threshold is met</li>
          <li>Automatic cancellation if the online count drops below the threshold</li>
          <li>Public voting through <code>/wg vote &lt;mode&gt;</code></li>
          <li>Allowed-mode filtering from config</li>
          <li>Deterministic result counting in allowed-list order</li>
          <li>Random selection only when no valid votes were cast</li>
          <li>Automatic mode switch and delayed countdown start after resolution</li>
        </ul>

        <div class="callout">
          <strong>Practical result</strong>
          <p>Taken together, the rewards and voting layers mean WGEvents already has a real event-server automation backbone: players can decide the next mode publicly, and the server can respond automatically to match actions without hardcoding all outcomes into the gamemode classes.</p>
        </div>
      `,
  resources: [
    {
      title: "Commands",
      text: "See the admin and public command paths used for reloads and voting.",
      url: "#wgevents-commands",
      label: "Open commands page"
    },
    {
      title: "Gamemodes",
      text: "See where kill and score events originate.",
      url: "#wgevents-gamemodes",
      label: "Open gamemodes page"
    },
    {
      title: "WGEvents overview",
      text: "Return to the WGEvents overview page.",
      url: "#wgevents",
      label: "Back to overview"
    }
  ],
  videos: [],
  history: [
    {
      date: "2026-04-08",
      text: "Added dedicated documentation for rewards.yml automation and idle-state public voting."
    }
  ]
};
